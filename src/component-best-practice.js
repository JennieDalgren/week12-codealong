export const ComponentName = () => {
  // 1. hooks which define variables
  // useState, useParams

  // 2. effect hooks
  // useEffect

  // 3. callback functions
  // handleSubmit, handleClick

  // 4. conditional rendering
  // if *this* retun *that*

  // 5. final render
  // render which happens if nothing stopped it above
}
